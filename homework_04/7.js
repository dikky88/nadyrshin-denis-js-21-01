//7. На вход дана строка, вернуть через alert все числа (десятичные разделяются сиволом ".")

const regexp = /[0-9\.-]+/g;

const str = "аоо уо532о оа968лу2д с3дтс3.0п аоп4,99клл"

alert(str.match(regexp))